{
  "name": "temporal-workflows",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --build",
    "build.watch": "tsc --build -w --preserveWatchOutput",
    "lint": "eslint ."
  },
  "dependencies": {
    "minio": "^7.0.26",
    "temporalio": "0.19.x",
    "zod": "^3.14.2"
  },
  "devDependencies": {
    "@tsconfig/node16": "^1.0.0",
    "@types/minio": "^7.0.12",
    "typescript": "^4.4.2"
  }
}
